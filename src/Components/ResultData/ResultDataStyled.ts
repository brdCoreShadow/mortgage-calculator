import styled from "@emotion/styled";

export const ResultDataCon = styled.div``;
export const ResultDataContentCon = styled.div`
  margin-bottom: 24px;

  & > h3 {
    margin-bottom: 16px;

    font-size: 24px;
    line-height: 1.25;

    color: #fff;
  }

  & > p {
    font-size: 16px;
    line-height: 1.5;
    font-weight: 500;

    color: #9abed5;
  }
`;

export const DigitsCon = styled.div`
  padding-top: 24px;
  padding-bottom: 24px;
  padding-left: 16px;
  padding-right: 16px;

  background-color: rgba(0, 0, 0, 0.25);

  border-top: 1px solid #d8db2f;
  border-radius: 8px;
`;

export const MonthlyRepayments = styled.div`
padding-bottom: 16px;

border-bottom: 1px solid rgba(154, 190, 213, 0.25)
;

  & > h4 {
    margin-bottom: 8px;

    font-size: 16px;
    line-height: 1.5;
    font-weight: 500;

    color: #9abed5;
  }

  & > span {
    font-size: 40px;
    font-weight: 700;

    color: #d8db2f;
  }
`;

export const Total = styled.div`
padding-top: 16px;

& > h4{
     margin-bottom: 8px;

    font-size: 16px;
    line-height: 1.5;
    font-weight: 500;

    color: #9abed5;
}

& > span{
    font-size: 24px;
    font-weight: 700;
    line-height: 1.25;

    color: #fff;
}
`